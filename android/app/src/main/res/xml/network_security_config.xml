<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Configuração Global: Proibir HTTP sem criptografia por padrão.
         fortivus.xyz usa certificado Let's Encrypt válido via Traefik/ACME,
         então a validação padrão contra os CAs do sistema é suficiente. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
